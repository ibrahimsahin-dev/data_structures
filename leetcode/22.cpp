#include <iostream>
#include <vector>
#include <stack>
#include <cmath>
#include <algorithm>
using namespace std;
vector<string> generateParenthesis(int n) {
    //"(())()"
        srand(time(0));
        
    }