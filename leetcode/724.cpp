#include <iostream>
#include <vector>
#include <stack>
#include <cmath>
#include <algorithm>
#include <numeric>
using namespace std;
// Input: nums = [2,1,-1]
// Output: 0

int pivotIndex(vector<int>& nums) {
        int left=0;
        int right=0;
        if(nums.size()==1)
            return 0;
        for(int i=nums.size()-1;i>=0;i--)
        {
            
        }
    }